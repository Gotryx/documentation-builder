# App Entrypoints Module

Módulo de inicialização híbrido do GoTryx Documentation Builder.

## Componentes
* `cli.py`: Fornece a interface de automação de ferramentas (CLI), ideal para execução via linha de comando em pipelines. Suporta saídas totalmente estruturadas em JSON.
